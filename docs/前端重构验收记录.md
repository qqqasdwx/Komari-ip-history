# 前端重构验收记录

## 01. 建立基线

阶段目标：记录当前 React SPA 的页面状态，作为后续 Tailwind 与 shadcn/ui 重构的视觉和行为对比基线。

本阶段 UI 改动：无。仅新增可复用 Playwright 基线截图脚本。

测试数据：

- 通过开发种子数据覆盖节点列表、无数据节点、多 IP 历史、多快照对比和长名称密集数据。
- 本轮截图使用 `开发种子-多IP历史` 和 `开发种子-多快照对比`。
- 可用 `docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/seed-dev-nodes.sh` 重新生成。

执行命令：

```bash
cd web && node --check playwright/capture-frontend-baseline.mjs
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
```

截图路径：

| 页面 | 截图 |
| --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/01-baseline/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/01-baseline/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/01-baseline/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/01-baseline/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/01-baseline/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/01-baseline/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/01-baseline/settings-integration-desktop.png` |

人工检查结果：

- 所有截图均已查看，页面不是 loading、空白页、错误页或骨架屏。
- 节点列表同时覆盖有数据和无数据节点；移动端能展示完整节点卡片。
- 详情页、历史页和对比页均显示真实种子报告和历史变化。
- 接入配置页显示当前 `loader` 和完整内联版 Header 内容。

遗留风险：

- 本阶段只建立基线，不改变页面结构。
- 公开访问和 Komari 默认/PurCarte 注入截图由 `run-e2e.sh` 继续覆盖，后续嵌入相关重构阶段再写入专门对比记录。

## 02. 引入 UI 基础设施

阶段目标：加入 shadcn/ui 风格基础设施，先不替换现有页面，降低后续页面重构风险。

本阶段改动：

- 新增 `web/src/react-preview/lib/utils.ts`，提供 `cn()`。
- 新增基础组件：`Button`、`Input`、`Label`、`Card`、`Badge`。
- 新增依赖：`class-variance-authority`、`clsx`、`tailwind-merge`、`@radix-ui/react-slot`、`@radix-ui/react-label`、`lucide-react`。
- 修复 E2E 过程中发现的 Komari 登录返回弹窗时序问题：通过 resume 恢复的弹窗会忽略滞后的 `open-standalone` 消息，避免 overlay 被误关闭。
- 页面行为和现有路由不变。

执行命令：

```bash
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && npm run build'
cd web && node --check playwright/capture-frontend-baseline.mjs
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && FRONTEND_REFACTOR_STAGE=02-ui-infra IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
docker compose -f compose.dev.yml exec -T workspace go test ./...
docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/run-e2e.sh
```

截图对比：

| 页面 | 改前 | 改后 |
| --- | --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/01-baseline/login-desktop.png` | `web/playwright-output/frontend-refactor/02-ui-infra/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/01-baseline/nodes-desktop.png` | `web/playwright-output/frontend-refactor/02-ui-infra/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/01-baseline/nodes-mobile.png` | `web/playwright-output/frontend-refactor/02-ui-infra/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/01-baseline/node-detail-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/02-ui-infra/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/01-baseline/history-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/02-ui-infra/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/01-baseline/compare-multi-snapshot-desktop.png` | `web/playwright-output/frontend-refactor/02-ui-infra/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/01-baseline/settings-integration-desktop.png` | `web/playwright-output/frontend-refactor/02-ui-infra/settings-integration-desktop.png` |

人工检查结果：

- 已查看所有改后截图，页面均加载完成，不是 loading、空白页、错误页或骨架屏。
- 本阶段没有替换页面组件，预期无可见视觉变化；截图符合预期。
- 构建通过，新增组件可被 Tailwind v4 扫描并生成样式。
- 完整 E2E 通过，默认主题和 PurCarte 主题的游客禁用、游客允许、管理员登录返回弹窗均通过断言。
- 已查看本轮 `embed-default` 和 `embed-purcarte` 截图，均为真实 Komari 页面、toast 或弹窗，不是 loading、空白或错误页。

遗留风险：

- `npm install` 报告 1 个 moderate vulnerability，本阶段未运行会改锁文件的 `npm audit fix`。
- 基础组件尚未接入业务页面，后续替换时仍需逐页截图和 Playwright 验收。

## 03. 拆分前端壳层组件

阶段目标：开始拆分 `App.tsx`，先抽离无业务改动的通用壳层组件，保持页面行为和视觉不变。

本阶段改动：

- 新增 `components/toast.tsx`，迁移 `pushToast` 和 `ToastViewport`。
- 新增 `components/layout/app-loading.tsx`，迁移全局加载页。
- 新增 `components/layout/embed-frame-shell.tsx`，迁移嵌入主题、PurCarte 玻璃参数和公开页 iframe 壳层。
- 新增 `components/layout/page-header.tsx` 和 `components/layout/sidebar-section.tsx`，迁移页头与侧边栏分组。
- `App.tsx` 保留路由和页面业务逻辑，未修改接口参数、权限判断、日期转换、路由跳转或 Playwright 数据属性。

测试数据：

- 本轮截图继续使用开发种子数据，覆盖有数据、无数据、多 IP 历史、多快照对比和长名称密集数据。
- 完整 E2E 会重建种子数据，并同时覆盖默认主题 Komari 与 PurCarte 主题 Komari 注入流程。

执行命令：

```bash
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && npm run build'
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && FRONTEND_REFACTOR_STAGE=03-app-split IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
docker compose -f compose.dev.yml exec -T workspace go test ./...
docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/run-e2e.sh
```

截图对比：

| 页面 | 改前 | 改后 |
| --- | --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/02-ui-infra/login-desktop.png` | `web/playwright-output/frontend-refactor/03-app-split/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/02-ui-infra/nodes-desktop.png` | `web/playwright-output/frontend-refactor/03-app-split/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/02-ui-infra/nodes-mobile.png` | `web/playwright-output/frontend-refactor/03-app-split/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/02-ui-infra/node-detail-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/03-app-split/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/02-ui-infra/history-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/03-app-split/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/02-ui-infra/compare-multi-snapshot-desktop.png` | `web/playwright-output/frontend-refactor/03-app-split/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/02-ui-infra/settings-integration-desktop.png` | `web/playwright-output/frontend-refactor/03-app-split/settings-integration-desktop.png` |

嵌入截图：

| 场景 | 截图 |
| --- | --- |
| 默认主题游客允许 | `web/playwright-output/embed-default/guest-allowed.png` |
| 默认主题游客禁用 | `web/playwright-output/embed-default/guest-blocked.png` |
| PurCarte 游客允许 | `web/playwright-output/embed-purcarte/guest-allowed.png` |
| PurCarte 游客禁用 | `web/playwright-output/embed-purcarte/guest-blocked.png` |

人工检查结果：

- 已查看所有 `03-app-split` 改后截图，页面均加载完成，不是 loading、空白页、错误页或骨架屏。
- 节点列表仍同时展示有数据和无数据节点；移动端卡片布局未被拆分影响。
- 详情、历史和快照对比页面仍展示真实种子报告、变化记录和多快照对比内容。
- 接入配置页仍展示公开访问开关、loader 版和完整内联版代码。
- 已查看默认主题和 PurCarte 嵌入截图，弹窗、toast 和 PurCarte 玻璃背景均正常。

遗留风险：

- 本阶段只做壳层级拆分，主要页面组件仍集中在 `App.tsx`；后续拆分页面时仍需逐页验收。

## 04. 拆分节点与历史小组件

阶段目标：继续降低 `App.tsx` 复杂度，抽离纯展示和小交互组件，不改变页面视觉、数据请求或路由行为。

本阶段改动：

- 新增 `components/common/search-box.tsx`，迁移节点搜索框。
- 新增 `components/node/status-pill.tsx`、`node-detail-loading.tsx`、`node-page-error.tsx`、`target-tabs.tsx`。
- 新增 `components/history/history-pagination.tsx`，迁移历史分页控件。
- `App.tsx` 从 3748 行降到 3494 行；页面数据 hooks、接口调用、URL query 处理仍留在原位置。
- 保留所有现有 className、按钮文案、数据属性和交互参数。

测试数据：

- 本轮继续使用开发种子数据，覆盖节点列表有数据/无数据、多 IP 目标、历史分页、多快照对比和移动端长名称场景。
- 完整 E2E 重建种子数据，并覆盖默认主题 Komari 与 PurCarte 主题 Komari 注入流程。

执行命令：

```bash
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && npm run build'
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && FRONTEND_REFACTOR_STAGE=04-component-split IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
docker compose -f compose.dev.yml exec -T workspace go test ./...
docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/run-e2e.sh
```

截图对比：

| 页面 | 改前 | 改后 |
| --- | --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/03-app-split/login-desktop.png` | `web/playwright-output/frontend-refactor/04-component-split/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/03-app-split/nodes-desktop.png` | `web/playwright-output/frontend-refactor/04-component-split/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/03-app-split/nodes-mobile.png` | `web/playwright-output/frontend-refactor/04-component-split/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/03-app-split/node-detail-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/04-component-split/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/03-app-split/history-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/04-component-split/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/03-app-split/compare-multi-snapshot-desktop.png` | `web/playwright-output/frontend-refactor/04-component-split/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/03-app-split/settings-integration-desktop.png` | `web/playwright-output/frontend-refactor/04-component-split/settings-integration-desktop.png` |

嵌入截图：

| 场景 | 截图 |
| --- | --- |
| 默认主题游客允许 | `web/playwright-output/embed-default/guest-allowed.png` |
| 默认主题游客禁用 | `web/playwright-output/embed-default/guest-blocked.png` |
| PurCarte 游客允许 | `web/playwright-output/embed-purcarte/guest-allowed.png` |
| PurCarte 游客禁用 | `web/playwright-output/embed-purcarte/guest-blocked.png` |

人工检查结果：

- 已查看所有 `04-component-split` 改后截图，均为加载完成页面，不是 loading、空白页、错误页或骨架屏。
- 节点搜索框、状态徽标、目标 IP tabs、历史分页控件和节点错误/加载组件均保持原视觉。
- 多 IP 详情页仍显示两个目标 IP；历史页仍显示第 1/17 页和分页控件；对比页仍显示时间线和左右报告。
- 已查看默认主题和 PurCarte 嵌入截图，游客允许弹窗与游客禁用 toast 均正常。

遗留风险：

- 本阶段仍未拆出页面级组件和数据 hooks，`App.tsx` 中还保留主要业务页面逻辑。

## 05. 拆分数据 Hooks 与工具函数

阶段目标：把页面数据加载 hooks 和纯工具函数移出 `App.tsx`，为后续页面级拆分做准备。

本阶段改动：

- 新增 `hooks/node-data.ts`，迁移 `useNodePageData`、`useAllNodeHistoryData`、`useNodeHistoryEvents`、`useNodeHistoryFieldOptions` 和 `usePublicNodePageData`。
- 新增 `lib/history-date.ts`，迁移历史时间预设、时间 query 转换和筛选范围描述。
- 新增 `lib/display-format.ts`，迁移容量格式化和历史保留天数文案。
- 新增 `lib/embed-navigation.ts`，迁移 Komari 返回 URL、独立页 URL 和 iframe 消息发送工具。
- 新增 `lib/route-label.ts`，迁移顶部栏路由标题映射。
- `App.tsx` 从 3494 行降到 2871 行；请求路径、query 参数、错误处理、路由行为和文案保持不变。

测试数据：

- 本轮继续使用开发种子数据，覆盖有数据、无数据、多 IP 历史、多快照对比和移动端长名称密集数据。
- 完整 E2E 重建种子数据，并覆盖默认主题 Komari 与 PurCarte 主题 Komari 注入流程。

执行命令：

```bash
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && npm run build'
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && FRONTEND_REFACTOR_STAGE=05-hooks-helpers IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
docker compose -f compose.dev.yml exec -T workspace go test ./...
docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/run-e2e.sh
```

截图对比：

| 页面 | 改前 | 改后 |
| --- | --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/04-component-split/login-desktop.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/04-component-split/nodes-desktop.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/04-component-split/nodes-mobile.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/04-component-split/node-detail-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/04-component-split/history-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/04-component-split/compare-multi-snapshot-desktop.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/04-component-split/settings-integration-desktop.png` | `web/playwright-output/frontend-refactor/05-hooks-helpers/settings-integration-desktop.png` |

嵌入截图：

| 场景 | 截图 |
| --- | --- |
| 默认主题游客允许 | `web/playwright-output/embed-default/guest-allowed.png` |
| 默认主题游客禁用 | `web/playwright-output/embed-default/guest-blocked.png` |
| PurCarte 游客允许 | `web/playwright-output/embed-purcarte/guest-allowed.png` |
| PurCarte 游客禁用 | `web/playwright-output/embed-purcarte/guest-blocked.png` |

人工检查结果：

- 已查看所有 `05-hooks-helpers` 改后截图，均为加载完成页面，不是 loading、空白页、错误页或骨架屏。
- 节点列表仍展示 7 个种子节点，并包含有数据和无数据状态。
- 多 IP 详情、历史分页、快照对比时间线和接入配置页均保持原视觉与数据展示。
- 已查看默认主题和 PurCarte 嵌入截图，游客允许弹窗、游客禁用 toast 和 PurCarte 玻璃背景均正常。

遗留风险：

- 本阶段没有拆出页面级组件，`App.tsx` 仍包含登录页、节点页、历史页、对比页和设置页主体。

## 06. 拆分入口与桥接页面

阶段目标：开始页面级拆分，先抽出依赖较少的入口和嵌入桥接页面，继续保持视觉与行为不变。

本阶段改动：

- 新增 `pages/login-page.tsx`，迁移登录页。
- 新增 `pages/connect-page.tsx`，迁移 Komari 接入桥接页。
- 新增 `pages/embed-bridge-page.tsx`，迁移打开独立页面的嵌入桥接提示页。
- 新增 `pages/embed-admin-access-bridge.tsx`，迁移嵌入管理员未登录桥接页。
- `App.tsx` 从 2871 行降到 2663 行；节点列表、详情、历史、对比和设置页主体仍保留在原文件。
- 登录、接入注册、管理员返回 Komari 弹窗、独立页 URL 生成和 iframe 消息发送逻辑保持不变。

测试数据：

- 本轮继续使用开发种子数据，覆盖有数据、无数据、多 IP 历史、多快照对比和移动端长名称密集数据。
- 完整 E2E 重建种子数据，并覆盖默认主题 Komari 与 PurCarte 主题 Komari 注入流程。

执行命令：

```bash
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && npm run build'
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && FRONTEND_REFACTOR_STAGE=06-entry-pages IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
docker compose -f compose.dev.yml exec -T workspace go test ./...
docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/run-e2e.sh
```

截图对比：

| 页面 | 改前 | 改后 |
| --- | --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/05-hooks-helpers/login-desktop.png` | `web/playwright-output/frontend-refactor/06-entry-pages/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/05-hooks-helpers/nodes-desktop.png` | `web/playwright-output/frontend-refactor/06-entry-pages/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/05-hooks-helpers/nodes-mobile.png` | `web/playwright-output/frontend-refactor/06-entry-pages/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/05-hooks-helpers/node-detail-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/06-entry-pages/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/05-hooks-helpers/history-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/06-entry-pages/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/05-hooks-helpers/compare-multi-snapshot-desktop.png` | `web/playwright-output/frontend-refactor/06-entry-pages/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/05-hooks-helpers/settings-integration-desktop.png` | `web/playwright-output/frontend-refactor/06-entry-pages/settings-integration-desktop.png` |

嵌入截图：

| 场景 | 截图 |
| --- | --- |
| 默认主题游客允许 | `web/playwright-output/embed-default/guest-allowed.png` |
| 默认主题游客禁用 | `web/playwright-output/embed-default/guest-blocked.png` |
| PurCarte 游客允许 | `web/playwright-output/embed-purcarte/guest-allowed.png` |
| PurCarte 游客禁用 | `web/playwright-output/embed-purcarte/guest-blocked.png` |

人工检查结果：

- 已查看所有 `06-entry-pages` 改后截图，均为加载完成页面，不是 loading、空白页、错误页或骨架屏。
- 登录页仍显示默认账号密码输入和登录按钮。
- 节点列表、详情、历史和对比页面显示真实种子数据，未受入口页拆分影响。
- 已查看默认主题和 PurCarte 嵌入截图，游客允许弹窗、游客禁用 toast 和 PurCarte 玻璃背景均正常。
- 完整 E2E 覆盖了默认主题和 PurCarte 的游客禁用、游客允许和管理员登录返回弹窗。

遗留风险：

- 本阶段只拆入口和桥接页；核心业务页面仍在 `App.tsx`，后续需要继续拆 `NodesPage`、详情页、历史页、对比页和设置页。

## 07. 拆分设置页面

阶段目标：继续页面级拆分，把设置区的接入配置、历史保留和用户页面从 `App.tsx` 移出，保持视觉、路由、权限处理和接口行为不变。

本阶段改动：

- 新增 `pages/integration-page.tsx`，迁移接入配置页和 Header 复制逻辑。
- 新增 `pages/history-retention-page.tsx`，迁移历史保留配置页。
- 新增 `pages/admin-page.tsx`，迁移用户资料页。
- 新增 `lib/clipboard.ts`，复用剪贴板复制 helper。
- 更新 `playwright/capture-frontend-baseline.mjs`，从本阶段起截图覆盖历史保留和用户页，并在截图前移开鼠标，避免 hover 状态污染验收图。
- `App.tsx` 从 2663 行降到 2142 行；设置页路由、请求路径、toast 文案和未登录处理保持不变。

测试数据：

- 本轮继续使用开发种子数据，覆盖空节点、有目标无数据、单 IP 无历史、单 IP 历史、多 IP 历史、多快照对比、长名称与密集数据节点。
- 完整 E2E 重建种子数据，并覆盖默认主题 Komari 与 PurCarte 主题 Komari 注入流程。
- 为补齐设置页改前对比，使用 `cea5dd3` 临时工作树补采 `06-entry-pages` 的历史保留和用户页截图。

执行命令：

```bash
cd web && node --check playwright/capture-frontend-baseline.mjs
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && npm run build'
docker compose -f compose.dev.yml exec -T workspace sh -lc 'cd /workspace/web && FRONTEND_REFACTOR_STAGE=07-settings-pages IPQ_PUBLIC_BASE_URL=http://127.0.0.1:8090 node playwright/capture-frontend-baseline.mjs'
docker compose -f compose.dev.yml exec -T workspace go test ./...
docker compose -f compose.dev.yml exec -T workspace sh /workspace/deploy/dev/workspace/run-e2e.sh
```

截图对比：

| 页面 | 改前 | 改后 |
| --- | --- | --- |
| 登录页 | `web/playwright-output/frontend-refactor/06-entry-pages/login-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/login-desktop.png` |
| 节点列表桌面 | `web/playwright-output/frontend-refactor/06-entry-pages/nodes-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/nodes-desktop.png` |
| 节点列表移动端 | `web/playwright-output/frontend-refactor/06-entry-pages/nodes-mobile.png` | `web/playwright-output/frontend-refactor/07-settings-pages/nodes-mobile.png` |
| 多 IP 节点详情 | `web/playwright-output/frontend-refactor/06-entry-pages/node-detail-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/node-detail-multi-ip-desktop.png` |
| 多 IP 历史记录 | `web/playwright-output/frontend-refactor/06-entry-pages/history-multi-ip-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/history-multi-ip-desktop.png` |
| 多快照对比 | `web/playwright-output/frontend-refactor/06-entry-pages/compare-multi-snapshot-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/compare-multi-snapshot-desktop.png` |
| 接入配置 | `web/playwright-output/frontend-refactor/06-entry-pages/settings-integration-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/settings-integration-desktop.png` |
| 历史保留 | `web/playwright-output/frontend-refactor/06-entry-pages/settings-history-retention-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/settings-history-retention-desktop.png` |
| 用户 | `web/playwright-output/frontend-refactor/06-entry-pages/settings-user-desktop.png` | `web/playwright-output/frontend-refactor/07-settings-pages/settings-user-desktop.png` |

嵌入截图：

| 场景 | 截图 |
| --- | --- |
| 默认主题游客允许 | `web/playwright-output/embed-default/guest-allowed.png` |
| 默认主题游客禁用 | `web/playwright-output/embed-default/guest-blocked.png` |
| PurCarte 游客允许 | `web/playwright-output/embed-purcarte/guest-allowed.png` |
| PurCarte 游客禁用 | `web/playwright-output/embed-purcarte/guest-blocked.png` |

人工检查结果：

- 已查看所有 `07-settings-pages` 改后截图，均为加载完成页面，不是 loading、空白页、错误页或骨架屏。
- 历史保留和用户页改前/改后截图均已查看，导航高亮、表单、统计信息和按钮状态正常。
- 节点列表仍展示 7 个种子节点，并覆盖有数据和无数据状态；移动端长名称节点仍能截断显示。
- 多 IP 详情、历史分页和多快照对比仍展示真实种子报告、字段变化、时间线和收藏快照状态。
- 接入配置页仍展示接入地址、游客只读开关、loader 版和完整内联版代码。
- 已查看默认主题和 PurCarte 嵌入截图，游客允许弹窗、游客禁用 toast 和 PurCarte 玻璃背景均正常。

遗留风险：

- 本阶段仍未拆出节点列表、节点详情、历史记录和快照对比页面主体，`App.tsx` 还保留核心业务页面逻辑。
