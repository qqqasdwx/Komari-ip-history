# 当前工作进度

更新时间：2026-03-28

## 1. 当前结论

项目阶段 1 的主链路已经基本打通，当前代码库已经具备可演示、可继续迭代的最小闭环：

- 独立 Go 后端服务已建立。
- 独立 Web 管理前端已建立。
- `Komari` 注入链路已打通，且不修改官方源码。
- 管理员登录、节点接入、节点列表、节点详情、历史页、系统配置页已可用。
- 展示字段全局开关已可用。
- 节点详情页“最近变化”摘要已可用。
- 节点详情页与历史页的字段级变化明细第一版已可用。
- 变化规则第一版已收口，已区分“重点变化”和“辅助变化”。
- 重点变化规则现已支持在系统配置页中全局调整。
- 独立变化视图已可用。
- 变化视图筛选能力已可用。
- 变化视图趋势表达第一版已可用。
- 变化视图趋势区口径切换与展示上限说明已可用。
- 重点变化规则已支持细化到字段路径。
- 用户主视图已开始按“当前结果 + 历史变化”重构。
- 用户主视图已进一步压缩成更接近结果页的结构。
- 页面视觉风格已继续向 Komari 靠拢。
- 后台外壳已改成更接近 Komari 管理后台的左侧导航骨架。
- 顶部“节点 / 配置”页签已移除。
- 配置页首屏已收口为“接入 Komari”，其余配置先折叠隐藏。
- 节点级 reporter token 与真实上报接口已可用。
- 容器化开发环境与 GitHub Actions 基础 CI 已落下。

## 2. 最近一次已完成提交

最近一次已提交 commit：

- `7829e09 feat: focus ip quality result pages`

这次提交完成了以下内容：

- 进一步压缩用户页信息量。
- 让详情页只突出重点变化。
- 让历史页只保留记录和变化本身。
- 把列表摘要收成最短风险分项视图。

本轮刚完成并准备提交的功能是：

- 把后台外壳从顶部页签改成 Komari 风格左侧导航。
- 去掉“节点 / 配置”这种首页式顶部一级导航。
- 把配置页首屏收回到“接入 Komari”，更多设置折叠隐藏。

## 3. 当前已经完成的阶段 1 功能

### 3.1 后端

- Go + Gin + Gorm + SQLite 基础架构。
- 单管理员模型与 session 登录。
- 节点注册、查询、删除。
- 当前结果与历史记录存储。
- 展示字段配置存储。
- 节点 reporter token 生成与轮换。
- 节点结果上报接口。

### 3.2 前端

- 登录页。
- 节点列表页。
- 节点详情页。
- 历史页。
- 系统配置页。
- Header 复制：
  - loader 版
  - 完整内联版
- 节点详情结构化展示：
  - 当前状态
  - 最近变化
  - 历史入口
  - 节点接入配置
- 变化展示第一版：
  - 分组级变化统计
  - 字段级变化明细
  - `旧值 -> 新值` 展示
  - 重点变化 / 辅助变化
- 变化规则配置：
  - 全局 `secondary_paths`
  - 默认 `Meta`
  - 系统配置页可修改
  - 支持顶层分组规则
  - 支持字段路径规则
- 变化视图：
  - 独立路由
  - 只聚焦变化本身
  - 可在详情页/历史页/变化视图之间切换
  - 只看重点变化
  - 只看有变化记录
  - 按分组筛选
  - 明确返回入口

### 3.3 注入与集成

- 通过 `Komari` 自定义 header 注入少量 JS。
- 注入按钮位于 `Komari` 节点详情页。
- 用户可在 `Komari` 节点详情页主动添加节点。
- 可从 `Komari` 打开本服务嵌入视图。

### 3.4 开发与验证

- `compose.dev.yml` 开发环境可用。
- `workspace` 容器内完成依赖安装、构建、验证。
- Playwright 注入验证脚本已可用。
- Playwright 阶段 1 主流程脚本已可用。
- GitHub Actions CI 已可运行基础构建与 E2E。

## 4. 当前已验证状态

最近一轮已验证通过的内容包括：

- `go build ./cmd/ipq`
- `cd web && npm run build`
- `node playwright/verify-injection.mjs`
- `node playwright/verify-stage1-flows.mjs`

最近一轮验证确认：

- 注入链路未回归。
- 节点详情页可显示 reporter 配置区。
- 可通过 reporter token 调用真实上报接口。
- 上报后详情页摘要会更新。
- 上报后历史记录条数会增加。
- 历史页与字段隐藏逻辑未回归。
- 详情页已能看到字段级变化项。
- 历史页已能看到字段级变化项。
- 详情页已能看到“重点变化”。
- 历史页已能看到“重点变化”和“辅助变化”分层。
- 设置页修改变化规则后，详情页和历史页会按新规则更新说明文案和分类结果。
- 变化视图已能展示字段级变化，并会跟随变化规则配置同步更新。
- 变化视图筛选已经能真实改变记录数量、变化明细和空状态。
- 变化视图趋势区已经能跟随筛选联动，并在无可统计数据时进入空状态。
- 趋势区已经能区分“当前筛选下全部趋势”和“只看重点变化趋势”。
- 趋势区已经有明确排序口径和截断提示，不再是无说明聚合。
- 字段级变化规则已经在详情页、历史页、独立变化视图和趋势区统一生效。
- 当前主视图已去掉大部分内部元数据和阶段性说明文案。
- 系统配置页已经开始把“接入 Komari”放在首位，高级项折叠收纳。
- 列表、详情、历史三页已经比上一版更接近“结果页”而不是“分析台”。
- 视觉层现在也已经比之前更接近 Komari，而不是一套独立设计语言。

## 5. 当前代码状态判断

按目前仓库状态，可以认为：

- 阶段 1 主骨架已完成。
- 当前不是“从零开始搭架子”的状态，而是进入“围绕变化体验聚合、优先级口径和阶段 2 设计”的状态。
- “IP 质量变化”已经有独立入口、可配置规则、基础筛选和第一版趋势表达，阶段 1 的变化查看主链路已经比较完整。
- 阶段 1 当前剩余更多是体验收口和分析边界，不再是主链路缺失问题。
- 当前“重点变化”已经不是只能按顶层分组粗分，阶段 1 的规则表达能力已基本够用。
- 现在更明显的工作重点已经从“补功能”转成“把界面收成真正可用的用户产品”。
- 当前最关键的不再是删信息，而是决定“结果页究竟要多像原始 IPQuality 页面”。
- 目前最大的剩余问题已经从“风格不像”变成“结果内容本身是否还要更贴近原始 IPQuality 展示顺序”。

## 6. 下一步建议优先级

我认为接下来更值得做的方向是：

1. 继续收口“IP 质量变化”核心能力
   - 决定后续是否要支持更多变化状态
   - 决定字段级规则后续是否需要更强的冲突/覆盖优先级定义
   - 决定是否还需要保留或重做趋势表达

2. 收口阶段 1 剩余体验细节
   - 检查嵌入视图与独立页之间是否还存在行为差异
   - 检查详情页、历史页、变化页是否还能继续减信息量
   - 检查规则配置页是否还需要搜索或折叠能力
   - 决定是否要进一步贴近 IPQuality 原始字段顺序和文案
   - 检查标题、按钮文案是否还需要继续贴近 Komari 语气

3. 补更多真实上报场景约束
   - 错误提示
   - 非法 token 响应
   - 空数据上报处理
   - 生产环境地址配置说明

## 7. 当前阻塞情况

当前没有工程层面的硬阻塞。

真正还需要继续拍板的，主要是业务层问题：

- “IP 质量变化”最终要按哪些字段定义。
- 趋势区后续到底保留、隐藏，还是改成更轻的统计入口。
- 字段级规则未来是否要支持更细的覆盖策略，例如“父路径已设为辅助，但子路径强制回到重点”。
- 当前结果页是否要继续向原始 IPQuality 展示风格靠拢，而不是沿用现有分组标题。
- 是否要继续模拟 Komari 首页那种“顶部概览 + 列表卡片”的节奏到更多页面。
- 阶段 1 收尾后，阶段 2 是先做变化分析，还是先做更多节点接入细节。

## 8. 相关文档

- `docs/项目理解.md`
- `docs/已确认事项.md`
- `docs/待确认事项.md`
- `docs/阶段1.md`
- `docs/开发环境.md`

## 9. 最新未提交变更

本轮变更聚焦“不要抄 Komari 首页，直接贴近 Komari 后台”。

- 登录后进入的主界面，已经从顶部页签结构改成左侧导航 + 右侧工作区。
- 节点页顶部不再放“节点 / 配置”页签，只保留当前页面相关操作。
- 节点列表首屏继续只保留 IP 质量结果，不再把配置入口当成主内容。
- 配置页首屏只保留接入 Komari 所需内容：
  - 复制 loader 版
  - 复制完整内联版
  - 查看当前路径和环境
- 字段展示、变化规则、管理员设置已统一收进“更多设置”折叠区，先隐藏，不再抢首屏注意力。

本轮已验证：

- `docker exec -i ipq-workspace-dev sh -lc "cd /workspace/web && npm run build"`
- `docker exec -i ipq-workspace-dev sh -lc "cd /workspace/web && node playwright/verify-injection.mjs"`
- `docker exec -i ipq-workspace-dev sh -lc "cd /workspace/web && node playwright/verify-stage1-flows.mjs"`

本轮额外确认：

- 运行中的前端开发服务已经刷新到新结构。
- 当前节点页 DOM 已确认出现左侧“节点结果 / 接入配置”导航。
- 验证产生的测试节点已清理：
  - 本服务节点数 `0`
  - Komari 客户端数 `0`

## 10. 最新未提交变更

本轮变更聚焦“已接入节点的详情结果，要直接贴近 IPQuality 的原始报告观感”。

- 当前 IP 质量结果区已从分组卡片改成深色终端报告风格。
- 结果展示顺序改成更接近 IPQuality：
  - 基础信息
  - IP 类型与风险评分
  - 风险因子
  - 流媒体与服务
  - 邮局检测
- `Head / Info / Type / Factor / Score / Media / Mail` 已纳入结构化识别，不再把 `Info / Type / Factor` 丢进“其他字段”。
- 服务商名称和关键字段名称已按 IPQuality 常见写法修正：
  - `IP2Location`
  - `AbuseIPDB`
  - `DB-IP`
  - `TikTok`
  - `Disney+`
  - `AmazonPV`
- Playwright 阶段 1 脚本已同步更新，适配“更多设置”折叠结构，避免误报回归。

本轮已验证：

- `docker exec -i ipq-workspace-dev sh -lc "cd /workspace/web && npm run build"`
- `docker exec -i ipq-workspace-dev sh -lc "cd /workspace/web && node playwright/verify-injection.mjs"`
- `docker exec -i ipq-workspace-dev sh -lc "cd /workspace/web && node playwright/verify-stage1-flows.mjs"`

本轮额外确认：

- 已用 `references/IPQuality/res/output.json` 造出示例节点并实际查看详情页。
- 当前详情页结果区已不再是白底统计卡片，而是接近 IPQuality 终端报告的网页化渲染。
- 演示节点已开始清理，避免污染当前开发环境。
